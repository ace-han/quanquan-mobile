define([
    './nl2br.filter'
], function () {
	// filter just name it in camelCase with prefix 
	// like: module: xxx, filter: yyy, then name the filter: xxxYyy(no hypen dash)
    'use strict';
});